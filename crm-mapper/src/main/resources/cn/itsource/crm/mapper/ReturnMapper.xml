<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.itsource.crm.mapper.ReturnMapper">

    <insert id="insert" parameterType="return" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO t_return
        (
        decs,
        operationId,
        returnTime,
        sn
        ) VALUES (
        #{decs},
        #{operationId},
        #{returnTime2},
        #{sn}
        )
    </insert>


</mapper>